# Hosted frontend defaults to an explicit demo. No secret belongs in VITE_* variables.
# For local FastAPI via Vite's proxy, set this to /api then restart npm run dev.
# For deployment, use your separately hosted HTTPS FastAPI service URL ending in /api.
VITE_API_BASE_URL=
